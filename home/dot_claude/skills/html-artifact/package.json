{
  "name": "html-artifact",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite src/showcase.html",
    "prebuild": "rm -rf dist",
    "build": "for entry in css showcase spec report prototype dashboard; do ENTRY=$entry vite build; done",
    "build:css": "ENTRY=css vite build",
    "preview": "vite preview",
    "test": "node test/verify.mjs"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.8",
    "daisyui": "^5.0.35",
    "tailwindcss": "^4.1.8",
    "vite": "^6.3.5",
    "vite-plugin-singlefile": "^2.1.0"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^18.0.5"
  }
}
